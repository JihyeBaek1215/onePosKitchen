version: 0.0
os: linux
files:
  - source: ./order-0.0.1-SNAPSHOT.jar
    destination: /home/ec2-user/build/
hooks:
  BeforeInstall:
    - location: /beforeInstall.sh
      timeout: 120
      runas: root
  AfterInstall:
    - location: /afterInstall.sh
      timeout: 120
      runas: root
